# agent opcode (https://github.com/dannyben/opcode)

## Quality

test: BUNDLE_USER_HOME=/tmp/slimview-bundle BUNDLE_USER_CACHE=/tmp/slimview-bundle/cache bundle exec rspec "$@"
#? Run the RSpec test suite

lint: rubocop --cache false "$@"
#? Run RuboCop with the global executable

check:
  agent test
  agent lint
#? Run tests and lint
