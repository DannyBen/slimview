# Slimview Agent Notes

Slimview is a small Ruby gem for previewing Slim templates through a Sinatra app.

Use the `agent` command for routine repo checks:

- `agent test` runs the RSpec suite.
- `agent lint` runs RuboCop.
- `agent check` runs both.

Update agent.op.conf aith any additional reusable commands as needed.
